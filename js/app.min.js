const year=(new Date).getFullYear();document.getElementById("year").textContent=year,document.querySelectorAll("[data-toggle]").forEach(e=>{e.addEventListener("click",()=>{document.getElementById(e.dataset.toggle).classList.toggle("show")})});const openDialog=document.getElementById("openDialog"),dialog=document.getElementById("myDialog");openDialog&&dialog&&openDialog.addEventListener("click",()=>dialog.showModal());